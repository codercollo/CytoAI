cytoai/
│
├── cmd/
│   └── cytoai/
│       └── main.go              # Entry point. Wires config, DB, router, starts HTTP server.
│                                 # (mirrors simplebank / Alex Edwards' /cmd/<app>/main.go convention)
│
├── internal/                    # Private application code — not importable by other projects
│   ├── api/
│   │   ├── server.go             # Router setup, middleware chain
│   │   ├── handlers_score.go      # POST /v1/score, GET /v1/score/:id
│   │   ├── handlers_ingest.go     # POST /v1/telematics, POST /v1/repayments (CSV/JSON ingestion)
│   │   ├── handlers_health.go     # GET /healthz, /readyz
│   │   └── middleware.go          # Auth, logging, request ID, rate limiting
│   │
│   ├── domain/
│   │   ├── battery.go             # Battery, TelemetryReading entities
│   │   ├── rider.go               # Rider, Loan, RepaymentEvent entities
│   │   └── score.go               # CytoScore, BatteryHealthIndex, RepaymentRiskIndex
│   │
│   ├── risk/
│   │   ├── scoring.go             # Combines BHI + RRI into final CytoScore
│   │   ├── mlclient.go            # HTTP client to the Python ML sidecar
│   │   └── rules.go               # Deterministic guardrails / sanity bounds on model output
│   │
│   ├── db/
│   │   ├── postgres.go            # Connection pool setup (pgx)
│   │   ├── queries/                # sqlc-generated or hand-written queries
│   │   └── migrations/             # SQL migration files (golang-migrate)
│   │
│   ├── config/
│   │   └── config.go              # Env/YAML config loading (Viper)
│   │
│   └── auth/
│       └── apikey.go              # Partner API-key auth for lender/operator clients
│
├── pkg/                          # Public code, safe to import externally
│   ├── cytoaiclient/          # Go client SDK for the Cyto AI API (for partner integrations)
│   │   └── client.go
│   └── csvutil/
│       └── csvutil.go             # Shared CSV parsing/validation helpers
│
├── test/                         # Integration / end-to-end tests (unit tests live beside source)
│   ├── integration/
│   │   ├── score_flow_test.go
│   │   └── ingest_flow_test.go
│   └── testdata/
│       ├── sample_telematics.csv
│       └── sample_repayments.csv
│
├── configs/
│   ├── config.example.yaml
│   └── config.local.yaml          # gitignored
│
├── docs/
│   ├── spec.md                    # Full technical specification (data, model, API, deployment)
│   ├── data-sources.md            # Catalogue of public datasets used for MVP bootstrap
│   └── architecture.png           # (optional) system diagram export
│
├── examples/
│   └── partner-integration/       # Sample script showing a lender calling the scoring API
│       └── score_example.go
│
├── api/
│   └── openapi.yaml               # OpenAPI 3.0 contract for the scoring/ingestion API
│
├── web/                           # Nuxt 4 / Vue 3 dashboard (lenders/operators view scores)
│   ├── app.vue
│   ├── nuxt.config.ts
│   ├── pages/
│   │   ├── index.vue               # Portfolio overview
│   │   ├── riders/[id].vue         # Individual rider/battery score detail
│   │   └── upload.vue              # Manual CSV upload UI for pilot partners
│   ├── components/
│   │   ├── ScoreGauge.vue
│   │   ├── DegradationChart.vue
│   │   └── UploadDropzone.vue
│   ├── composables/
│   │   └── useCytoApi.ts
│   ├── public/
│   └── package.json
│
├── build/
│   ├── docker-compose.yml         # go-api + ml-sidecar + postgres + caddy, local & prod parity
│   ├── Dockerfile.api              # Go API image
│   ├── Dockerfile.ml               # Flask ML sidecar image
│   ├── Dockerfile.web              # Nuxt web image
│   └── Caddyfile                   # Reverse proxy + TLS termination (Contabo deployment)
│
├── scripts/
│   ├── fetch_public_data.sh        # Downloads public bootstrap datasets (battery + credit proxy)
│   ├── generate_synthetic.py       # Generates synthetic Kenya-context telematics/repayment rows
│   ├── migrate.sh                  # Runs DB migrations
│   └── deploy_contabo.sh           # Pulls latest image, restarts via docker-compose on VPS
│
├── vendor/                        # Go module vendor directory (optional, for reproducible builds)
│
├── ml/                            # Python/Flask ML sidecar — lightweight model, small data, fast iteration
│   ├── app/
│   │   ├── __init__.py
│   │   ├── config.py
│   │   ├── models/
│   │   │   ├── __init__.py
│   │   │   ├── battery_health_model.py    # Loads/serves BHI model (<2MB joblib artifact)
│   │   │   └── repayment_risk_model.py    # Loads/serves RRI model (<2MB joblib artifact)
│   │   ├── routes/
│   │   │   ├── __init__.py
│   │   │   └── score_routes.py            # POST /predict/battery, POST /predict/repayment
│   │   ├── schemas/
│   │   │   ├── __init__.py
│   │   │   └── score_schema.py            # Marshmallow/pydantic request-response schemas
│   │   ├── services/
│   │   │   ├── __init__.py
│   │   │   └── scoring_service.py         # Feature engineering + inference orchestration
│   │   └── tests/
│   │       ├── __init__.py
│   │       └── test_score_routes.py
│   ├── training/
│   │   ├── train_battery_model.py         # Trains BHI model on data/battery/
│   │   ├── train_repayment_model.py       # Trains RRI model on data/repayment/
│   │   └── notebooks/
│   │       └── exploration.ipynb
│   ├── data/                              # Small, gitignored except sample/ — see data-sources.md
│   │   ├── battery/                       # NASA/CALCE public cycle-life CSVs (~10–50MB total)
│   │   ├── repayment/                     # Public credit-risk proxy CSVs (~5–20MB)
│   │   ├── synthetic/                     # Generated Kenya-context sample rows (<5MB)
│   │   └── sample/                        # Tiny checked-in samples for tests/demo (<1MB, committed)
│   ├── artifacts/                         # Trained model files (.joblib), each <5MB
│   ├── run.py                             # Flask entry point
│   └── requirements.txt
│
└── .github/
    └── workflows/
        ├── test.yml                # Lint + unit/integration tests on PR (Go + Python + Nuxt)
        └── deploy.yml              # Build images, push, SSH-deploy to Contabo on merge to main
