name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch: {}

env:
  REGISTRY: ghcr.io

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    outputs:
      api_image: ${{ steps.tags.outputs.api_image }}
      web_image: ${{ steps.tags.outputs.web_image }}
    steps:
      - uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Compute lowercase image names + tags
        id: tags
        run: |
          API_NAME=$(echo "${{ github.repository }}-api" | tr '[:upper:]' '[:lower:]')
          WEB_NAME=$(echo "${{ github.repository }}-web" | tr '[:upper:]' '[:lower:]')
          echo "api_name=$API_NAME" >> "$GITHUB_OUTPUT"
          echo "web_name=$WEB_NAME" >> "$GITHUB_OUTPUT"
          echo "api_image=${{ env.REGISTRY }}/$API_NAME:${{ github.sha }}" >> "$GITHUB_OUTPUT"
          echo "web_image=${{ env.REGISTRY }}/$WEB_NAME:${{ github.sha }}" >> "$GITHUB_OUTPUT"

      - name: Build + push API image (Go API + ML sidecar)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: build/Dockerfile.api
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ steps.tags.outputs.api_name }}:${{ github.sha }}
            ${{ env.REGISTRY }}/${{ steps.tags.outputs.api_name }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build + push Web image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: build/Dockerfile.web
          push: true
          build-args: |
            NUXT_API_BASE=http://api:8080
          tags: |
            ${{ env.REGISTRY }}/${{ steps.tags.outputs.web_name }}:${{ github.sha }}
            ${{ env.REGISTRY }}/${{ steps.tags.outputs.web_name }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to Contabo VPS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.CONTABO_HOST }}
          username: ${{ secrets.CONTABO_USER }}
          key: ${{ secrets.CONTABO_SSH_KEY }}
          script: |
            export API_IMAGE="${{ needs.build-and-push.outputs.api_image }}"
            export WEB_IMAGE="${{ needs.build-and-push.outputs.web_image }}"
            echo "${{ secrets.GHCR_PAT }}" | docker login ghcr.io -u "${{ secrets.GHCR_USERNAME }}" --password-stdin
            cd /opt/cytoai
            grep -q '^API_IMAGE=' .env 2>/dev/null && sed -i "s|^API_IMAGE=.*|API_IMAGE=$API_IMAGE|" .env || echo "API_IMAGE=$API_IMAGE" >> .env
            grep -q '^WEB_IMAGE=' .env 2>/dev/null && sed -i "s|^WEB_IMAGE=.*|WEB_IMAGE=$WEB_IMAGE|" .env || echo "WEB_IMAGE=$WEB_IMAGE" >> .env
            bash scripts/deploy_contabo.sh
